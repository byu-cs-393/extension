{
  "name": "cs393-buddy-extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Chrome extension client for CS 393 Buddy. Content scripts are bundled from src/content/ into src/ by scripts/build-content-scripts.js \u2014 run `npm run build` after editing them. Cloud Functions live in functions/ with their own package.json.",
  "scripts": {
    "build": "node scripts/build-content-scripts.js",
    "build:watch": "node scripts/build-content-scripts.js --watch",
    "build:check": "node scripts/build-content-scripts.js --check && node scripts/check-paths.js",
    "test": "vitest",
    "test:run": "vitest run",
    "ci": "npm run build:check && npm run test:run",
    "check:paths": "node scripts/check-paths.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "vitest": "^2.1.0"
  }
}
